CONFIG_ENTROPY_GENERATOR=y
CONFIG_ENTROPY_DEVICE_RANDOM_GENERATOR=y

# I2C and sensor subsystem for BME280
CONFIG_I2C=y
CONFIG_SENSOR=y

# Flash for NVS-backed config persistence (nRF52840 internal flash)
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_FLASH_PAGE_LAYOUT=y
